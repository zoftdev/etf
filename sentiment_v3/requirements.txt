# Core dependencies
pyyaml>=6.0
anthropic>=0.40.0
openai>=1.12.0

# Web search (optional, choose one or more)
ddgs>=9.0.0  # Free, no API key needed (formerly duckduckgo-search)
# google-api-python-client>=2.0  # For Google Custom Search
# serpapi>=0.1.0  # For SerpAPI (requires API key)

# Optional utilities
requests>=2.31.0
python-dateutil>=2.8.2
