# Decision: dip_buy — strategy params only (no exit_rules; SMC handles exit; spread in simulation.yaml)
dip_buy:
  trend_days: 200
  dip_days: 5
  slope_lookback_days: 20
  use_slope_filter: false
  min_dip_pct: 0.0
